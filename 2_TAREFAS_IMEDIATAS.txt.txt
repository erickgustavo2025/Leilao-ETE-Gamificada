# ORDEM DE SERVIÇO: EXECUÇÃO IMEDIATA

Você deve ler o código atual e resolver os seguintes épicos na ordem:

**ÉPICO 1: A Morte do XP e Recompensas (Frontend)**
1. Arquivo alvo: `frontend/src/pages/dashboard/QuestBoard.tsx`.
2. O Backend (`getSecondaryQuests`) não envia mais `xp`. Ele envia `rewardItems`.
3. Remova toda renderização de XP da interface e tipagens.
4. No `SideQuestCard`, ao lado do valor de PC$, adicione a renderização do pacote de itens. Se a missão tiver itens, mostre o ícone `Package` (lucide-react) e o texto "Contém Itens".

**ÉPICO 2: O Bug do WebP (Full-Stack)**
1. As imagens dos itens pararam de carregar. Nós migramos tudo para `.webp`.
2. Verifique os componentes de Loja (Beco Diagonal), Inventário e os Models/Seeds do Backend. 
3. Troque qualquer referência `.png` ou `.jpg` de itens para `.webp`.

**ÉPICO 3: Sistema de Aprovação Manual (Full-Stack)**
1. Missões com `validationMethod: 'MANUAL_ADMIN'` geram requisições pendentes.
2. Backend: Crie a lógica para o aluno enviar a missão para aprovação. O status deve ficar pendente.
3. Backend: Crie rotas para o Admin listar pendências, Aprovar e Rejeitar. A aprovação deve entregar o Loot (PC$ e Itens) na mochila correta.
4. Frontend: Crie o componente `AdminApprovals.tsx` (Adicione no AdminLayout). O professor deve ver as solicitações e poder aprovar/rejeitar com `useMutation` (atualização em tempo real).

**ÉPICO 4: Integração de Benefícios na Mochila (Backend)**
1. Arquivo alvo: `backend/src/controllers/questController.js`.
2. Garanta que o Loot da missão respeite o `Classroom.js`.
3. ATENÇÃO: O model `Classroom` exige que o campo `origin` seja exatamente 'PREMIO'.